<!DOCTYPE html>
<html>
  <head>
    <title>annie-walsh.com</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://kit.fontawesome.com/cd39e7ba65.js" crossorigin="anonymous"></script>
  </head>
  <body>
      <%= render 'shared/navbar' %>
      <%= yield %>
      <%= render 'shared/footer' %>
  </body>
   <div class="bg-black border border-white- text-white px-4 py-3 rounded relative" role="alert">
      <strong class="font-bold">Mobile friendly version coming soon!</strong>
      <span class="block sm:inline"> But not today which is <% time = Time.now %>
      <%= time.strftime("%A the %d of %B, %Y") %></span>
      </div>
</html>
